# userscripts

Placeholder.

This repository is reserved for a future public mirror of a small set of
Violentmonkey userscripts. Nothing is published here yet.

The intended shape, when it is populated: one `.user.js` install unit per file
at the repo root, no build step — a userscript manager copies each file
verbatim, so whatever is committed is exactly what runs.

## Licence

MIT — see [`LICENSE`](LICENSE).
